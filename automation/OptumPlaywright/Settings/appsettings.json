{
  "_comment": "Template only — no secrets belong here. Local overrides go in appsettings.local.json (gitignored). Runtime values are actually sourced from environment variables set by the active .runsettings file (see local.runsettings / headed-local.runsettings).",
  "BaseUrl": "https://www.optum.com/en/",
  "Headless": true,
  "DefaultTimeoutMs": 15000,
  "NavigationTimeoutMs": 30000,
  "ViewportWidth": 1440,
  "ViewportHeight": 900
}
